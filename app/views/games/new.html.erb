<%= simple_form_for [ @game ] do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :image_url %>
  <%= f.input :condition, :collection => %w[good fair poor] %>
  <%= f.submit "Add game", class: "btn btn-primary" %>
<% end %>
